os:
  - linux
services:
  - docker
language: node_js
node_js:
  - "8.9.0"
addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable
cache:
  yarn: true
  directories:
    - $HOME/.yarn-cache
    - client/shell/node_modules
before_install:
  - export CHROME_BIN=/usr/bin/google-chrome
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  # Repo for Yarn
  - npm install -g @angular/cli
install:
  - cd client/app && npm install
script:
  - ng test --watch=false
notifications:
  webhooks:
    on_success: change
    on_failure: always
    on_start: false
