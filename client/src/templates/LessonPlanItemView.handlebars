<div class="lesson-language">{{lessonPlan_subject_full}}</div>
<div class="lesson-info">Class {{lessonPlan_grade}}<br>Week {{lessonPlan_week}} Day{{lessonPlan_day}}</div>
<div class="lesson-text">{{{lessonPlan_lesson_text}}}</div>
{{#each elements}}
    {{#if image}}
        <div><img src="lesson_plan_media/{{{fileName}}}"/></div>
        <!--{{{fileName}}}-->
        <!--{{{fileSize}}}-->
        <!--{{{fileType}}}-->
        <!--{{{name}}}-->
        <!--{{{element}}}-->
    {{else if audio}}
        <audio controls>
            <source src="lesson_plan_media/{{{fileName}}}" type="{{{fileType}}}">
            Your browser does not support the audio tag.
        </audio>
    {{else if video}}
        <video controls>
            <source src="lesson_plan_media/{{{fileName}}}" type="{{{fileType}}}">
        </video>
    {{else}}
       <div>{{{html}}}</div>
    {{/if}}
{{/each}}


