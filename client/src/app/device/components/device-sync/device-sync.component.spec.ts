import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeviceSyncComponent } from './device-sync.component';

/* @TODO
describe('DeviceSyncComponent', () => {
  let component: DeviceSyncComponent;
  let fixture: ComponentFixture<DeviceSyncComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeviceSyncComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeviceSyncComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
