class CameraEditView extends Backbone.View

  className : "CameraEditView"

  initialize: ( options ) ->
    @model = options.model
    @parent = options.parent

  render: -> # do nothing

  save: -> # do nothing
  
  isValid: -> true
